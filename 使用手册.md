# 海战棋使用手册

浏览器直接打开 `main.html`。

## 赛前准备

> 好的赛前准备可以避免非常多的麻烦。

比赛前几天让双方选手派代表依次（不要一起来）到比如学生会办公室等有电脑无其他无关人员的地方配置布局文件，建议命名为对应的队名，如 `宇宙无敌暴龙战士队.json` 。

另外请操作人员自己额外配置一个与双方不同的布局文件，建议命名为 `测试布局.json` 。

为了防止文件配置出问题，请提前使用双方的布局文件以及正式比赛的题目在安全电脑处演练，确保所有题目所有布局都测试过没有任何问题。有条件一定要提前去举办决赛的地方用那边的电脑完整过一遍流程，重点看题目有没有超出显示范围。

决赛时，设置里三项都填完再点保存，然后才能开始。队名、题库、两份舰队配置缺一样都不行。

建议开赛前全屏浏览器，按键盘上的 `F11` 即可。

开场有大约五秒过场，这期间鼠标会消失，等棋盘出来再动。

## 开火

**键盘开火（推荐，让选手按照先列后行报数，然后操作人员直接用键盘点击即可）**：

1. `A` 选左棋盘，`B` 选右棋盘。对应队名会**闪红**。
2. 先按列号，再按行号。`1`～`9` 是第 1～9，`0` 是第 10。
3. `Enter` 开火。
4. `Backspace` 一步步退：先退行，再退列，再取消选板。

**鼠标开火**：

1. 直接单击对应格子即可。

**注意：出于游戏公平性，开火无法回退，一旦点错无法撤销，请慎重操作！**

## 答题

进来先点「下一题」才会出第一题。当前这题没答完，不能进下一题。没答的时候可以点「跳过」，直接进下一题，不给炮弹。

左右两边的 A/B/C/D 是两队各自的作答区。点哪边，就算哪队答。

**如果点错题目了**：这题还没点「下一题」之前可以改：再点别的选项，或者改成另一队答。炮弹和连错会按改之前的状态重算，以最后一次为准。

**右键**收起 / 呼出题目面板。过场没结束、题目还没出来时右键没用。题目全部问完后面板会自己收起来，之后右键也呼不出来。

## 结算

中间标题那块的「结算」平时是藏着的，能点了再点。点完才进结果页，不会自己跳转。

两种情况会出结算按钮：

- 有一队三艘船全没了：那队输，对面赢。哪怕题目还没问完也一样。
- 题目问完了，双方炮弹也打光了，还没人全沉：按平局。

题目问完但还有炮弹时，继续打完。这几发里仍可能打沉对方，那就按击沉算，不是平局。

结果页放完音乐、彩带后，再点一下卡片会重放。

## 题库文件

使用 `importer.exe` 来配置题目，请使用 `txt` 格式文件来存放原来的题目，要求

题干和选项都认 LaTeX （这是写数学物理公式的好方法，不会写可以找 AI ）：`$...$`、`$$...$$`、`\[...\]`、`\(...\)`、`\begin{...}...\end{...}`。

只有题干允许配图，选项里写了也不会显示，建议把所有图片声明放在题目末尾以免渲染时出现多余空格。语法：

```text
!?文件名.png?!
```

文件放在 `resources/img/problems/`。名字只许字母、数字、下划线、短横线和扩展名，不能带路径。例如 `!?boat1.png?!` 可以，`!?a/b.png?!`、`!?不是图片?!` 不行，会原样显示。

## 舰队配置

布局文件用 `map_generator.html` 做。

撤销最多记 30 步。导出文件名没写 `.json` 会自动补。
